# EFPBD — eFootball tournaments Bangladesh

Starter site: frontend + Node/Express backend (SQLite).
See /frontend and /server.

## Local dev

### Frontend
Open `frontend/index.html` in browser, or run via a static server.

### Backend
cd server
npm install
npm start
Server runs at http://localhost:3000
